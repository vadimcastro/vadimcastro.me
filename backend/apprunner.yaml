version: 1.0
runtime: python3
build:
  commands:
    build:
      - python3 -m pip install fastapi uvicorn
run:
  command: python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: PYTHONPATH
      value: /app