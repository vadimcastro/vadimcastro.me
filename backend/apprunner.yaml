version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python3 basic_server.py
  network:
    port: 8080
  env:
    - name: ENVIRONMENT
      value: production