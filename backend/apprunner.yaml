version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "Starting build..."
      - ls -la
      - pwd
      - python3 --version
      - echo "Build complete"
run:
  command: python3 test_server.py
  network:
    port: 8080