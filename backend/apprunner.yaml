version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python3 -m uvicorn simple_test:app --host 0.0.0.0 --port 8080 --log-level info
  network:
    port: 8080
  env:
    - name: ENVIRONMENT
      value: production